import CommonGroundClient from '@/components/common-ground/CommonGroundClient';
import MainLayout from '@/components/MainLayout';

export default function CommonGroundPage() {
  return (
    <MainLayout>
      <CommonGroundClient />
    </MainLayout>
  );
}