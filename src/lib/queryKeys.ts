export const budget = {
  rollup: 'budget.rollup',
  hierarchy: 'budget.hierarchy',
  events: 'budget.events',
};

export const macro = 'macro';
export const presidents = 'presidents';

export const tax = {
  baseline: 'tax.baseline',
};